language: node_js
node_js: '12'
cache: npm
before_script:
  - npm run lint
before_deploy:
  - npm run generate
deploy:
  provider: pages
  local_dir: dist
  github_token: $GITHUB_TOKEN
  on:
    branch: master

